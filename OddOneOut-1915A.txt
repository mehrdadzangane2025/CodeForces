#include <bits/stdc++.h>
#include <iostream>
#include <algorithm>
#include <cmath>
#include <map>
#include <vector>
#include <string>
using namespace std;
#define len(n)  n.length()
#define ll long long
#define siz(x)  (int)x.size()
typedef pair<int, int> pairs;
int main()
{
    int n;
    cin>>n;
    int a[n];
    while(n--){
        cin>>a[0]>>a[1]>>a[2];

        sort(a, a+3);
        if(a[0] == a[1])
        {
            cout<<a[2]<<endl;
        }else{
            cout<<a[0]<<endl;
        }
    }
}